# 🔁 V-Looper on FFmpeg

**V-Looper on FFmpeg** — это набор скриптов, которые позволяют *насильно* заставить `ffmpeg` собрать повторяющееся (зацикленное) видео.  
Это чистая уловка, потому что **ни аппаратные кодировщики, ни сам ffmpeg для такой операции не предназначены**.

---

## ⚙️ Почему аппаратные кодировщики и сам ffmpeg «упираются»

### 1. Аппаратные кодировщики (включая VAAPI) не рассчитаны на монтаж  
Каждый аппаратный кодировщик — это жёсткая машина с трубопроводом, которая требует **строгого, предсказуемого и линейного потока данных**.  
VAAPI, как и NVENC/AMF/QSV:

* ожидает **один непрерывный поток без разветвлений**,  
* жёстко управляет своим буфером предсказаний,  
* не терпит ветвлений, перемоток, разрывов временных баз.

А ffmpeg, чтобы построить повторяющийся ролик, вынужден строить **ветвистый граф обработки**, где отрезки видео переплетаются через фильтры.  
Для железа это *чуждая* структура — его просто не создавали для подобной филигранной точности.

### 2. ffmpeg не умеет «шить» видео в один файл без промежуточных стадий  
Когда требуется получить корректную конкатенацию, ffmpeg **обязательно** ожидает разные файловые сущности, чтобы:

* пересчитать таймлайны,  
* заново собрать индекс,  
* не смешивать кадры в одном буфере.

То есть прямой цикл «вход ⇒ фильтр ⇒ выход ⇒ снова вход» — для него неродной процесс.  
Ему приходится каждый шаг буквально *разрывать* в файловую систему.

---

## 🧩 Итог

* **Аппаратное кодирование** — это инструмент для **быстрого, однонаправленного** кодирования, а не для промыслового монтажа и точных циклов.  
* **FFmpeg** — не «конкат для чайников», а швейцарский нож вокруг библиотек, который не обязан быть удобным для задач стыковки.

V-Looper — это не магия. Это **искусственный обход ограничений**, а не штатное применение технологий.

---

## 🐒 «Обезьянья работа», только автоматизированная

Скрипты в этом репозитории — это просто автоматизация рутинной и тягомотной последовательности:

1. На каждой итерации создаётся *копия* текущего видео.  
2. Порождается файл `list$i.txt` с очередным списком фрагментов.  
3. Вызывается `ffmpeg` с `-filter_complex`, который вяжет фрагменты в один ролик.  

Никакой магии, только аккуратные, повторяющиеся действия, превращённые в скрипт.

---

## ▶️ Использование скриптов

### `encode-vaapi.sh` — перекодирование в компактный HEVC  
Перекодировать входной файл во что-то поменьше (HEVC + AAC):
```
./encode-vaapi.sh input.mkv
```

Скрипт использует:

* VAAPI
* HEVC кодирование (`hevc_vaapi`)
* NV12 → VAAPI upload
* AAC 192k

### `video_pipeline.sh` — создание длинного «зацикленного» файла  
Запуск последовательности итераций:

```
for i in {1..6}; do
./video_pipeline.sh cpu_iterate $i
done
```

*Итог:* появится цепочка `long1.mkv → long2.mkv → … → long7.mkv`, где каждая следующая — результат предыдущей итерации.

---

## 📝 Лицензия

Проект распространяется по **GNU GPLv3**.


